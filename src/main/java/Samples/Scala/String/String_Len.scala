package Samples.Scala.String

object String_Len {

//----------------------------------------------------------
   def main(args: Array[String]) {
       
      var x = "Dot saw I was Tod If you have a good understanding on Java, then it will be very easy for you to learn Scala. The biggest syntactic difference between Scala and Java is that the ';' line end character is optiona";
      var len = x.length();
      
      println( "String Length is : " + len );
   }
//----------------------------------------------------------   
}